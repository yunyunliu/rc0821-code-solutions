/* exported maxValue */

function maxValue(stack) {
  // if (typeof stack.peek() === 'undefined') {
  //   return -Infinity;
  // }
  // let max = stack.peek();
  // while (typeof stack.peek() != 'undefined') {
  //   if (stack.peek() > max) {
  //     max = stack.pop();
  //   }
  // }
  // return max;
}
